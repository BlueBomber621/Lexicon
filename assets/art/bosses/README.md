# Boss seals

The wax-seal emblem for each boss debuff, drawn from `#icon-boss-<id>`.

- `viewBox="0 0 48 48"`.
- File naming: `icon-boss-<id>.svg` (e.g. `icon-boss-the-censor.svg`).
- Ids come from `BOSSES` (and `BOSSES_IMPERIAL`) in `js/content.js`.
